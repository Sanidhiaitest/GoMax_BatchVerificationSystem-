Drop real avatar photos here, named by person (lowercase, spaces -> hyphens):

  pargat-singh.png
  anshul.png
  ravi-sharma.png
  bobby.png
  raju.png
  varun.png

PNG or JPG both work, square (e.g. 200x200) looks best — the app crops
them into a circle automatically. Anyone without a file here falls back
to the colored-initials avatar, so it's safe to add these one at a time.

Same idea for product photos, once you have them — ask in chat for the
exact folder/filenames to use for those (they're looked up by
formulation code, e.g. /products/pt10.png).
